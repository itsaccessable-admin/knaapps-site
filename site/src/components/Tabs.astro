---
const { tabs, active } = Astro.props;
---

<div class="flex flex-wrap gap-2">
  {tabs.map((t) => (
    <a
      href={t.href}
      class={"badge " + (t.label === active ? "border-slate-400 text-slate-900" : "text-slate-700 hover:border-slate-300")}
    >
      {t.label}
    </a>
  ))}
</div>
